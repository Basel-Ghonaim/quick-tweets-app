/** What the server accepts for a picture, mirrored here so the screen offers
 *  nothing it would refuse. */
export const AVATAR_ACCEPT = "image/jpeg,image/png";
export const AVATAR_MAX_BYTES = 1024 * 1024;
