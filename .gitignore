# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
src/master
src/shared/components/propmt
dist
dist-ssr
*.local

# Server
server/.env
server/src/generated

# WI-5 legacy-cleanup sensitive artifacts (contain media tokens / storage keys) — never commit.
# The sanitized review report (wi5-review-report.json) is tokenless and safe to share/attach.
server/wi5-manifest*.json
server/wi5-run-report*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static
# Local scratch plans stay ignored, but the formal plans home (docs/plans/) is tracked.
plans
!docs/plans/
!docs/plans/**

# Local AI working drafts (never committed or pushed)
.project/
