import type { BaseVariantProps } from "../variant.types";

/**
 * Props for the DropzoneInput variant.
 *
 * Extends BaseVariantProps. No additional props needed yet —
 * drag & drop state (isDragOver) is internal to the variant.
 */
export type DropzoneInputProps = BaseVariantProps;
