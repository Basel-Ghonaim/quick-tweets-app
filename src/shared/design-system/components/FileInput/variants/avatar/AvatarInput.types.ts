import type { BaseVariantProps } from "../variant.types";

/**
 * Props for the AvatarInput variant.
 *
 * Extends BaseVariantProps. Uses avatarShape and avatarFill
 * from the base to determine visual style.
 * Single-file only — no multi-file support.
 */
export type AvatarInputProps = BaseVariantProps;
